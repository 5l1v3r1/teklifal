<h2>New Announcement</h2>

<div class="row">
  <div class="col-2 offset-1"><%= current_user.name %></div>
  <div class="col-8">
    <%= render 'form', announcement: @announcement %>

    <%= link_to 'Back', announcements_path %>

  </div>
</div>
