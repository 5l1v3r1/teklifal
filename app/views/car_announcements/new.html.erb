<%= content_for :container_top do %>
  <div class="top-image new-car"></div>
<% end %>

<%= title t(".title") %>

<%= session[:created_announcement] %>
<% unless user_signed_in? %>
  <%= render "announcements/stepper", step: :announcement %>
<% end %>
<div class="row">
  <div class="col-12 col-md-8">
    <%= page_header_with_info t(".title") do %>
      <a href="#" data-toggle="modal" data-target="#subscriptionsModal" class="d-inline-block d-md-none">
        <span class="subscribers-count"><%= @subscriptions.count %></span> abone
      </a>
    <% end %>
    <%= render 'form', content: @content %>
  </div>

  <div class="d-none d-md-block col-md-4">
    <%= render "subscriptions/subscriptions" %>
  </div>

</div>

<% if ask_to_continue? %>
  <%= render "announcements/modal" %>
<% end %>

<%= render "subscriptions/subscriptions_modal" %>