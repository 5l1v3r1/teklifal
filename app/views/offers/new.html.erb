<%= render 'announcements/announcement_card', announcement: @announcement %>

<%= page_header "New Offer" %>

<%= render 'form', offer: @offer %>

<%= link_to 'Show', @announcement %>
<%= link_to 'Back', @announcement %>
